# プロジェクト目標仕様

> このファイルは人間が記入するゴール仕様です。
> Claude はこのファイルを読んでループを実行します。

## 目標

{{最終的に何を実現したいかを1〜3文で記述}}

例:
> ユーザーが Markdown ファイルをアップロードすると、AI が要約・タグ付けして
> SQLite に保存する。API エンドポイント `/summarize` で呼び出せること。

## 完了条件（Done Criteria）

以下がすべて満たされたときにループを終了する:

- [ ] {{具体的なテストが通ること}}
- [ ] {{エンドポイントが動作すること}}
- [ ] {{必要なドキュメントが書かれていること}}

## スコープ外（やらないこと）

- {{ここに書かれたことは実装しない}}

## 技術スタック

- **言語**: {{TypeScript / Python など}}
- **フレームワーク**: {{Next.js / FastAPI など}}
- **DB**: {{SQLite / PostgreSQL など}}
- **テスト**: {{Vitest / pytest など}}

## 制約

- テストカバレッジ 70% 以上を維持する
- 外部APIキーはすべて環境変数から読む
- `main` ブランチへの直接コミットは禁止

## 参考資料

- {{設計ドキュメントのパスや URL}}
